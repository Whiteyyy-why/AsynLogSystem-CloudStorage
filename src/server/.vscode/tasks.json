{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-gdb-test",
      "type": "shell",
      "command": "make gdb_test",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": ["$gcc"],
      "group": "build"
    }
  ]
}